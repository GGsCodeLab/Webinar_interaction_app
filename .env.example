# ─── Database (SQLite) ───────────────────────────────────────────────────────
# Optional. Default: mypoll.db in project root.
# Railway: set to /data/mypoll.db when using a volume mounted at /data
# DATABASE_PATH="/data/mypoll.db"

# ─── NextAuth ─────────────────────────────────────────────────────────────────
AUTH_SECRET="run: openssl rand -base64 32"
# Optional: Google OAuth (if you add the provider)
# AUTH_GOOGLE_ID="your-google-client-id.apps.googleusercontent.com"
# AUTH_GOOGLE_SECRET="your-google-client-secret"

# App URL (required for auth redirects and Socket.IO client).
# Local:
NEXTAUTH_URL="http://localhost:3000"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
# Railway: set both to your deployment URL, e.g. https://your-app.up.railway.app

# ─── Set by Railway (no need to add) ──────────────────────────────────────────
# PORT, NODE_ENV, RAILWAY_PUBLIC_DOMAIN
